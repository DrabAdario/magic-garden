/** Configurable winter survival length. */
export const WINTER_DAY_COUNT = 10;

/** Coins per ration bought in the shop (growing seasons only). */
export const RATION_SHOP_PRICE = 10;

/** Health lost when you have no rations at the start of a winter day. */
export const STARVATION_DAMAGE = 22;

export const HEALTH_MAX = 100;
